on:
  pull_request:
  push:

jobs:
  firmware:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: DeterminateSystems/nix-installer-action@main
    - uses: DeterminateSystems/magic-nix-cache-action@main
    - name: Build firmware
      working-directory: firmware
      run: nix develop .#ci --command idf.py build

  hardware:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: DeterminateSystems/nix-installer-action@main
    - uses: DeterminateSystems/magic-nix-cache-action@main

    - name: Build interactive bom
      run: >
        nix shell --inputs-from . kicad-parts#interactive-html-bom --command \
          generate_interactive_bom --no-browser --layer-view F hardware/light-control.kicad_pcb --dest-dir public --name-format index
      env:
        INTERACTIVE_HTML_BOM_NO_DISPLAY: 1

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./hardware/public

  deploy:
    needs: hardware

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
